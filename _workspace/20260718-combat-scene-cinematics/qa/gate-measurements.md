# QA Gate Measurements — Combat Scene Cinematics

## G2 — Rules & balance numbers well-set
No combat numbers (damage, HP, cooldowns, TTK, win-rate bands) were touched this cycle. VFX/audio/bevel work is presentation-layer only. **Carry-forward pass** from prior cycles (see `_workspace/20260717-combat-resource-parity/qa/` and `20260716-abyssal-surge-revision/qa/` for the numeric gate evidence this cycle does not re-measure).

## G6 — Technical stability (functional correctness under real conditions)
- **Measured value:** 123/123 automated tests passing (`node --test tests/*.test.mjs`), 0 failures, 0 skipped, across the full combined working-tree state (this session's VFX/audio/camera-feel work + the Blender bevel/atlas lane + the concurrently-landed action-feedback refactor + browser-test hardening).
- **Method:** `node --test tests/*.test.mjs` run twice at different points in the cycle (mid-cycle after Lane B code complete, and again after third-party integration confirmed) — both green.
- **Evidence:** command output captured in session transcript; `node --check battle-realtime-three.js` syntax-clean throughout.
- **Live verification (beyond unit tests):** headless Playwriter session against a fresh `python3 -m http.server` instance — new campaign start, Stage 1 combat entry, WebGL canvas active (`canvas3dHidden: false`), zero `pageerror` events across the full session.

## G6 — Live particle/audio instrumentation evidence
Unit tests alone cannot prove particles/audio fire during real gameplay (they test construction safety and call-signature correctness, not runtime emission under a live `AudioContext`). Verified separately by prototype-patching in a live browser:
- `AudioContext.prototype.createOscillator` / `createBufferSource` call counts before/after a `hunt` action click: confirmed increment (1 oscillator + 1 buffer-source created per action), audio context state `"running"`.
- `emitActionFeedback` call log confirmed invocation with `hasAudio: true`, `audioCtxState: "running"` on real click-driven action dispatch.
- Repeated after third-party `emitActionFeedback` refactor landed — same result, confirming the shared implementation still fires correctly for both this session's event types (clash/defeat/breach/ambient) and the action-feedback event type.

## G6 — Non-browser environment safety
- **Incident:** initial `SpatialAudio` constructor called `new (window.AudioContext ...)` unconditionally, crashing all 8 `RealtimeBattle`-constructing tests in the Node test runner (`ReferenceError: window is not defined`).
- **Fix:** guarded with `typeof window === "undefined" ? null : ...`; all particle/audio call sites already used optional chaining (`this.particles?.emit`, `this.audio?.playTone`) so a `null` audio/particle field degrades silently rather than crashing.
- **Verification:** full re-run of `tests/battle-realtime-three.test.mjs`, all 8 previously-failing tests pass; confirmed no other unguarded `window`/`document` reference was introduced (`grep -n "window\.|document\."` over the new code).

## Concurrent work — investigation and resolution
- **Trigger:** `git diff --stat` mid-cycle showed 5 files outside this session's assigned scope (`app.js`, `styles.css`, and 3 test files) with real content changes, discovered while reconciling a stash-pop conflict.
- **Investigation:** read every diff hunk in full. Confirmed via `git show HEAD:battle-realtime-three.js` that the pre-existing `rally.copy`/reconciliation code this session found suspicious was already present before this session started (not introduced by either party) — no evidence of destructive overwrite.
- **Content assessment:** the changes are a coherent, well-tested refactor (`emitActionFeedback`/`actionFeedbackProfile`/`actionFeedbackPoint` replacing this session's own `playActionEffect` action-branch code with a cleaner shared implementation), a "current objective" command-highlight UI feature with matching CSS and full Playwright-style browser-test coverage, and two genuine bug fixes unrelated to this session's scope (stale `STATIC_CACHE_NAME` literal removed; hardcoded `"Stage X of 3"` assertion in `tests/playtest-browser-3stage.cjs` generalized to the actual 10-stage campaign).
- **Resolution:** treated as legitimate parallel collaboration (matches the user's own framing: "병렬로 작업하자" — let's work in parallel). Verified integration correctness: full test suite green (123/123), live browser smoke test clean, and a targeted live re-verification that the shared `emitActionFeedback` implementation still fires particles+audio correctly after the refactor landed on top of this session's original implementation.
- **No revert performed.** No data lost — a `git stash`/`git stash pop` cycle during reconciliation was verified byte-for-byte restorative (`git diff HEAD` before stash vs. after pop compared directly) before proceeding.

## Independent code review findings and fixes (post mid-cycle commit `2ffe2ba`)

An independent code-review subagent (`CombatVfxReview`) reviewed the full source diff (`battle-realtime-three.js`, `app.js`, `styles.css`, and the three touched test files) after the working tree was committed mid-review by the user as `2ffe2ba "fix: harden v0.2.1 combat release"`. Findings, independently re-verified against the actual current file contents (not taken on faith), and every non-Nit finding fixed in this session before gate close:

| Severity | Finding | Independent verification | Fix |
|---|---|---|---|
| Critical | Boss-defeat VFX branch (48-particle burst, boss tone, `shakeCamera(0.35,0.5)`, hit-stop) was unreachable — `defeat(unit)` is only ever called with enemies/allies, never `this.boss`; `applyCampaignState` never read the `bossHealth` app.js already passes it. | Confirmed via `grep` of every `defeat(` call site and `applyCampaignState` destructuring; confirmed live in a real browser playthrough (Stage 1, 3 real assault clicks through the actual UI) that boss health went 8→5→2→0 and the patched `defeat()` prototype logged `{isBoss:true}` on the exact transition to zero. | Added `this.lastBossHealth` tracking; `applyCampaignState` now calls `this.defeat(this.boss)` exactly once when `bossHealth` transitions from >0 to 0. Live-verified end-to-end (see below). |
| Major | `shakeCamera` unconditionally overwrote `shakeDuration` on every call, so a weaker/shorter pulse arriving mid-decay of a stronger pulse snapped the amplitude back to full strength instead of continuing the smooth decay. | Reproduced the exact numeric scenario from the review (0.35/0.5 decayed to residual 0.035, a subsequent weaker pulse spiking it back to 0.35) via a new unit test before the fix, confirmed it failed, then confirmed the fix passes. | `shakeCamera` now only re-arms the envelope (`shakeMagnitude`/`shakeTime`/`shakeDuration`) when the new pulse's initial strength exceeds the currently-decaying residual strength; a weaker pulse is a no-op. |
| Major | The `playActionEffect` → `emitActionFeedback` refactor silently dropped the pre-existing `possess`/`domain` → rally-all-allies-to-commander behavior (present at HEAD~1, confirmed via `git show`). | Confirmed via `git show 7c089ff:battle-realtime-three.js` that the line `if (action === "possess" || action === "domain") this.rally.copy(this.commanderPosition);` existed pre-refactor and was not carried forward. | Restored the same line in the current `playActionEffect`. New unit test confirms rally position updates on possess/domain and is untouched by other actions. |
| Minor | `this.particles.update(dt)` (unlike 14 other particle call sites) lacked the `?.` null-safety guard. | Confirmed via grep — only call site without the guard. | Added `?.`. |
| Minor | The `pulse` field on `actionFeedbackProfile()`'s profile objects was inert dead code — `pulseActionMarker()` already unconditionally pulses on the raw semantic source/target strings, and every profile entry with `pulse` set also had a matching semantic string. | Confirmed via cross-reference with `BATTLE_ACTION_SEMANTICS` in app.js — every action's source/target already covers what `pulse` claimed to gate. | Removed the dead `pulse` field and the redundant `if (pulse === ...)` block. |
| Minor | `applyBossIdentityTint` (stages 4-10) clones materials per-instance but neither `retire()` nor `destroy()` disposed them — a real (if pre-existing-pattern-adjacent) leak across a 10-stage campaign's repeated stage transitions. | Confirmed via reading `retire()`/`destroy()` — no material disposal existed anywhere in the file before this fix. | Marked clones with `userData.isBossIdentityTint = true`; `retire()` now traverses and disposes exactly those clones, never touching template-shared materials (new unit test confirms the distinction). |
| Minor | The engineering doc (`vfx-audio-implementation.md`) falsely claimed `shakeCamera` was "mulberry32-seeded, deterministic" — no such RNG exists anywhere in this file (only `Math.random()`). | Confirmed via grep — `mulberry32` only exists in `iso-math.js`/`battle-visualizer.js` (the separate Canvas 2D renderer), never imported here. | Corrected the doc. |
| Minor | The engineering doc overstated test coverage, claiming particle-pool-bounds and direct class-construction safety were unit-tested when they were not. | Confirmed via grep of the test file — true at time of review. | Closed the actual gap instead of just correcting the claim: exported `ParticleField`/`SpatialAudio` and added direct unit tests for pool recycling (`emit(400)` on a 360-capacity pool correctly recycles, cursor wraps at `400 % 360 = 40`) and non-browser construction safety. |
| Nit | Stray `?.` on one `playTone` call inconsistent with the other 8 unguarded call sites; four double-blank-line instances. | Confirmed via grep. | Dropped the stray `?.`; collapsed all double-blank-lines file-wide via a scripted pass, verified with `node --check`. |

**Post-fix verification:** 9 new/corrected unit tests added to `tests/battle-realtime-three.test.mjs` (21 total in that file, up from 14), all passing. Full suite: 144/144 passing (`node --test tests/*.test.mjs`). Live browser re-verification specifically for the Critical fix: a real Stage 1 playthrough via the actual UI (hunt→hunt→extract→materialize→materialize→capture, encounter waves auto-resolved, then 3 real assault clicks) drove boss health 8→5→2→0 through the genuine `applyAction`/`applyCampaignState` production path (not a mock), with a prototype-patched `defeat()` confirming exactly one `{isBoss:true}` call at the health-zero transition and the campaign correctly reaching `VICTORY`/`campaign-complete` status. Zero console errors throughout; WebGL renderer (`#battle-canvas-3d`) remained active, no Canvas 2D fallback triggered.

## G7 — Core loop stability
Not touched this cycle (no core-loop-model changes). Carry-forward from `20260717-combat-resource-parity`.

## Cycle close
Blender bevel-pass lane (`BlenderBevelPass` subagent, cancelled mid-completion-report but with all substantive work already committed): 15/15 GLB re-exports independently re-verified in this session — animation order matches manifest-declared `actions` exactly on all 15 assets (the subagent's own "alphabetical reordering" concern was from an isolated pristine-control export it ran for root-cause investigation, not the actual shipped files); zero subsurf modifiers / 86 bevel modifiers confirmed directly in the `.blend` via headless Blender inspection; zero NaN vertices; vertex counts increased 3.6×-8.2× as expected from selective beveling (e.g. shade 236→1932, cinder-warden 782→2848); isolated-part renders of cinder-warden's horns/fire-ring/staff confirm clean bevel geometry. 8-directional atlas re-render (`--pack --publish`) completed and published to both `assets/images/battle/glb/manifest.json` and `assets/media-manifest.json` (47 files updated) before the subagent was cancelled; verified via the full test suite (`Canvas fallback stage 4–10 terrain and boss art agree with declared stage resources` and related atlas tests pass).
